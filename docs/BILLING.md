# Billing — Revenue (Super Admin)

**Route:** `/billing` • **File:** `Billing.jsx` • **Table:** `subscriptions` (stub), `billing_plans`

## Status
Plans active; Stripe not wired beyond `stripe_*` columns. `GET /api/admin/billing` placeholder.

## Future
Gate white-label on `plan='enterprise'` eligibility check.

## HTML
`docs/SUPER_ADMIN_GUIDE.html#billing`
