# Settings (Super Admin & Tenant Admin)

**Route:** `/settings` • **File:** `Settings.jsx`

## Scope
- Super Admin: global `PLATFORM_HOST`, `APP_NAME`, instance settings
- Tenant Admin: own tenant branding fallback (use `/white-label` or `/design` for full wizard)

## HTML
`docs/SUPER_ADMIN_GUIDE.html#settings`
